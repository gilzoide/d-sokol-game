enum maxObjects = 8;
enum cameraStackSize = 2;

enum clearColor = [0.2, 0.2, 0.2, 1];

enum initialWindowWidth = 1024;
enum initialWindowHeight = 576;
enum windowTitle = "d-sokol-game";

enum windowAspectX = cast(float) initialWindowWidth / cast(float) initialWindowHeight;
enum windowAspectY = cast(float) initialWindowHeight / cast(float) initialWindowWidth;

enum hexagonSize = 0.7;
enum keyboardGridColumns = 9;
enum keyboardGridRows = 4;
